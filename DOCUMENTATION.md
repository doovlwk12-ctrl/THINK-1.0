# توثيق منصة فكرة - دليل شامل

## 📑 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [الميزات التفصيلية](#الميزات-التفصيلية)
3. [دليل المستخدم](#دليل-المستخدم)
4. [دليل المطور](#دليل-المطور)
5. [قاعدة البيانات](#قاعدة-البيانات)
6. [API Reference](#api-reference)
7. [التكاملات](#التكاملات)

---

## نظرة عامة

منصة فكرة هي منصة PWA متكاملة تربط العملاء بالمهندسين المعماريين. تهدف المنصة إلى تبسيط عملية التخطيط المعماري من خلال:

- تسهيل التواصل بين الأطراف
- توفير أدوات تفاعلية للتعديلات
- إدارة شاملة للطلبات والمشاريع
- نظام إشعارات فوري

### حالة المشروع (MVP)

**✅ الميزات المكتملة:**
- نظام المصادقة والتسجيل (NextAuth.js)
- إنشاء الطلبات مع استبيان متعدد الخطوات
- نظام الدفع المحاكي
- لوحة تحكم العميل (عرض الطلبات والحالة)
- لوحة تحكم المهندس (عرض الطلبات المخصصة وغير المخصصة)
- نظام المحادثة (Polling)
- رفع المخططات (PDF/صور) مع ضغط الصور تلقائياً
- إرسال المخططات للعملاء (تفعيل/إلغاء تفعيل)
- حفظ البيانات بين الخطوات (localStorage)
- أزرار التنقل (عودة، العودة للصفحة الرئيسية)
- حذف الملفات قبل الرفع
- PWA الأساسي (Service Worker، Manifest)
- **نظام التعديلات التفاعلي (الدبابيس على المخططات)** ✅
- **نظام الإشعارات (In-App Notifications)** ✅
- **شراء تعديلات إضافية** ✅
- **تمديد الموعد النهائي من قبل المهندس** ✅
- **تأكيد إكمال الطلب من قبل العميل** ✅
- **لوحة تحكم الإدارة (جزئياً: إدارة الباقات والطلبات)** ✅
- **نظام الأمان والاستقرار (Rate Limiting, Security Headers, Error Handling)** ✅

**⏳ الميزات المتبقية:**
- Push Notifications (Web Push API) - In-App Notifications موجودة
- إكمال لوحة تحكم الإدارة (إدارة المهندسين والإحصائيات الفعلية)
- نظام التقييمات
- الأرشيف التلقائي

---

## الميزات التفصيلية

### 1. نظام الباقات

#### أنواع الباقات

**الباقة الأساسية**
- السعر: 500 ريال
- عدد التعديلات: 2
- وقت التنفيذ: 7 أيام
- المميزات: مخطط واحد، دعم فني

**الباقة القياسية**
- السعر: 1000 ريال
- عدد التعديلات: 5
- وقت التنفيذ: 14 يوم
- المميزات: مخطط واحد، دعم فني، أولوية

**الباقة المميزة**
- السعر: 2000 ريال
- عدد التعديلات: 10
- وقت التنفيذ: 21 يوم
- المميزات: مخطط واحد، دعم فني، أولوية عالية، استشارة مباشرة

#### إدارة الباقات

- يمكن للإدارة تعديل:
  - الأسعار
  - عدد التعديلات
  - وقت التنفيذ
  - تفعيل/إلغاء تفعيل الباقات

### 2. نظام الطلبات

#### إنشاء الطلب

1. **اختيار الباقة**
   - العميل يختار الباقة المناسبة
   - يمكن تغيير الباقة قبل الدفع

2. **ملء الاستبيان**
   - نوع المشروع (سكني، تجاري، صناعي، مختلط)
   - المساحة الإجمالية
   - عدد الطوابق
   - عدد الغرف والحمامات
   - مواقف السيارات
   - المتطلبات الخاصة
   - الموقع (المدينة، الحي، العنوان)
   - الميزانية المتوقعة
   - الموعد المطلوب
   - ملاحظات إضافية

3. **الدفع**
   - طرق الدفع المتاحة:
     - بطاقة ائتمانية
     - مدى
     - Apple Pay
   - بعد الدفع الناجح، يتم إنشاء الطلب

#### حالات الطلب

- **PENDING** - في الانتظار (بعد الدفع، قبل تعيين المهندس)
- **IN_PROGRESS** - قيد التنفيذ (المهندس يعمل على الطلب)
- **REVIEW** - قيد المراجعة (تم رفع المخطط، في انتظار مراجعة العميل)
- **COMPLETED** - مكتمل (تم إكمال الطلب)
- **ARCHIVED** - في الأرشيف (بعد الإكمال)

### 3. نظام المحادثة

#### المميزات

- محادثة فورية بين العميل والمهندس
- ربط كل محادثة برقم طلب مميز
- إشعارات فورية عند وصول رسالة جديدة
- عرض المخططات المرفوعة في المحادثة
- إمكانية تحميل الملفات

#### الاستخدام

- كل طلب له محادثة خاصة
- يمكن للعميل والمهندس التواصل في أي وقت
- الرسائل محفوظة في قاعدة البيانات
- إشعارات عند الرسائل غير المقروءة

### 4. نظام التعديلات

#### كيفية العمل

1. **رفع المخطط**
   - المهندس يرفع المخطط (PDF أو صورة)
   - المخطط يظهر للعميل في المحادثة

2. **طلب التعديل**
   - العميل يضغط على "طلب تعديل"
   - يفتح صفحة المخطط التفاعلية

3. **إضافة الدبابيس**
   - العميل ينقر على المخطط في مكان التعديل
   - يظهر دبوس ملون
   - العميل يكتب ملاحظة التعديل
   - يمكن إضافة عدة دبابيس بألوان مختلفة

4. **إرسال طلب التعديل**
   - بعد إضافة جميع الدبابيس، العميل يرسل الطلب
   - يتم إرسال إشعار للمهندس
   - التعديلات المتبقية تقل بواحد

5. **معالجة التعديل**
   - المهندس يرى طلب التعديل مع الدبابيس
   - يبدأ بالعمل على التعديلات
   - يرفع المخطط المعدل
   - يتم إرسال إشعار للعميل

#### الألوان المتاحة

- أحمر - للأولويات العالية
- أزرق - للتعديلات العادية
- أخضر - للإضافات
- أصفر - للاستفسارات
- بنفسجي - للتغييرات الكبيرة
- وردي - للملاحظات

### 5. نظام الإشعارات

#### أنواع الإشعارات

**للعميل:**
- طلب قيد التنفيذ
- بدأ العمل على الطلب
- تم إرسال المخطط
- تم رفع المخطط المعدل
- رسالة جديدة من المهندس
- تم إكمال الطلب
- تمديد الموعد النهائي
- تنبيه: تعديلات قليلة متبقية

**للمهندس:**
- طلب جديد
- طلب تعديل جديد
- تم استلام طلب التعديل
- رسالة جديدة من العميل

#### طرق الإشعارات

1. **Push Notifications**
   - إشعارات المتصفح
   - تعمل حتى عند إغلاق التطبيق
   - تتطلب إذن المستخدم

2. **In-App Notifications**
   - إشعارات داخل التطبيق
   - تظهر في جرس الإشعارات
   - لا تتطلب إذن

3. **Email Notifications** (اختياري)
   - إشعارات بريد إلكتروني
   - للأحداث المهمة

**ملاحظة:** حالياً In-App Notifications مكتملة ✅. Push Notifications (Web Push API) قيد التطوير.

### 6. شراء تعديلات إضافية

#### كيفية العمل

1. **عند نفاد التعديلات**
   - إذا استخدم العميل جميع التعديلات المتاحة في الباقة
   - سيظهر زر "شراء تعديلات إضافية"

2. **اختيار عدد التعديلات**
   - العميل يختار عدد التعديلات (1-20)
   - السعر: 100 ريال لكل تعديل
   - يتم حساب السعر الإجمالي تلقائياً

3. **الدفع والإضافة**
   - بعد الدفع الناجح
   - يتم إضافة التعديلات للطلب
   - يتم تمديد الموعد النهائي تلقائياً (2 يوم لكل تعديل)
   - يتم إرسال إشعار للعميل

### 7. تمديد الموعد النهائي

#### للمهندس

- يمكن للمهندس تمديد الموعد النهائي للطلب
- يختار عدد الأيام الإضافية
- يتم تحديث الموعد النهائي في قاعدة البيانات
- يتم إرسال إشعار للعميل بالتمديد

### 8. تأكيد إكمال الطلب

#### للعميل

- بعد استلام المخطط النهائي وتنفيذ جميع التعديلات
- العميل يضغط "تأكيد إكمال الطلب"
- يتم تحديث حالة الطلب إلى `COMPLETED`
- يتم حفظ تاريخ الإكمال
- يتم إرسال إشعار للمهندس

### 9. لوحات التحكم

#### لوحة تحكم العميل

**المعلومات المعروضة:**
- قائمة جميع الطلبات
- حالة كل طلب
- عدد التعديلات المتبقية
- الموعد النهائي
- عدد الرسائل غير المقروءة

**الإجراءات المتاحة:**
- إنشاء طلب جديد
- فتح المحادثة
- طلب تعديل
- تأكيد إكمال الطلب
- تحميل المخططات

#### لوحة تحكم المهندس

**المعلومات المعروضة:**
- إجمالي الطلبات
- الطلبات في الانتظار
- الطلبات قيد التنفيذ
- الطلبات المكتملة
- عدد الرسائل غير المقروءة
- طلبات التعديل المعلقة

**الإجراءات المتاحة:**
- عرض تفاصيل الطلب
- بدء العمل على الطلب
- رفع المخطط
- فتح المحادثة
- معالجة طلبات التعديل
- تمديد الموعد النهائي
- إكمال الطلب

#### لوحة تحكم الإدارة

**المعلومات المعروضة:**
- إجمالي الطلبات
- إجمالي المستخدمين
- إجمالي الإيرادات
- المهندسين النشطين
- إحصائيات مفصلة

**الإجراءات المتاحة:**
- ✅ إدارة الباقات (إضافة، تعديل، حذف، تفعيل/تعطيل)
- ✅ عرض جميع الطلبات مع pagination
- ⏳ إدارة المهندسين (قيد التطوير)
- ⏳ عرض الفواتير (قيد التطوير)
- ⏳ إدارة المستخدمين (قيد التطوير)
- ⏳ الإحصائيات الفعلية (قيد التطوير)

---

## دليل المستخدم

### للعميل

#### إنشاء حساب جديد

1. اذهب إلى الصفحة الرئيسية
2. اضغط "إنشاء حساب"
3. املأ البيانات:
   - الاسم الكامل
   - البريد الإلكتروني
   - رقم الجوال
   - كلمة المرور
4. اضغط "إنشاء حساب"

#### إنشاء طلب جديد

1. من لوحة التحكم، اضغط "طلب جديد"
2. اختر الباقة المناسبة
3. املأ الاستبيان:
   - ابدأ بنوع المشروع
   - أكمل جميع الخطوات
   - راجع المعلومات قبل الإرسال
4. اضغط "إنشاء الطلب"
5. ادفع مقابل الباقة
6. انتظر تعيين المهندس

#### التواصل مع المهندس

1. من لوحة التحكم، اختر الطلب
2. اضغط "المحادثة"
3. اكتب رسالتك واضغط "إرسال"
4. يمكنك رؤية المخططات المرفوعة

#### طلب تعديل

1. بعد رفع المخطط، اضغط "طلب تعديل"
2. انقر على المخطط في مكان التعديل
3. اختر لون الدبوس
4. اكتب ملاحظة التعديل
5. كرر للدبابيس الأخرى
6. اضغط "إرسال طلب التعديل"

#### تأكيد إكمال الطلب

1. بعد استلام المخطط النهائي
2. تأكد من تنفيذ جميع التعديلات
3. اضغط "تأكيد إكمال الطلب"
4. قم بتقييم الخدمة

### للمهندس

#### استلام طلب جديد

1. ستصلك إشعار بطلب جديد
2. من لوحة التحكم، اضغط على الطلب
3. راجع بيانات الطلب كاملة
4. اضغط "بدء العمل على الطلب"

#### رفع المخطط

1. بعد الانتهاء من التخطيط
2. اذهب لصفحة الطلب
3. في قسم "رفع المخطط"
4. اختر الملف (PDF أو صورة)
5. اضغط "رفع المخطط"
6. سيتم إرسال إشعار للعميل

#### معالجة طلب التعديل

1. ستصلك إشعار بطلب تعديل
2. اضغط "عرض التفاصيل"
3. راجع الدبابيس والملاحظات
4. ابدأ بالعمل على التعديلات
5. بعد الانتهاء، ارفع المخطط المعدل
6. اضغط "تم معالجة التعديلات"

---

## دليل المطور

### إعداد بيئة التطوير

```bash
# 1. Clone المشروع
git clone <repository-url>
cd fekra-platform

# 2. تثبيت التبعيات
npm install

# 3. إعداد متغيرات البيئة
cp .env.example .env
# عدّل .env وأضف:
# - DATABASE_URL="file:./dev.db" (للـ MVP - SQLite)
# - NEXTAUTH_SECRET
# - NEXTAUTH_URL
# - VAPID keys (للإشعارات - اختياري)

# 4. إعداد قاعدة البيانات
npx prisma generate
npx prisma db push

# 5. (اختياري) إنشاء بيانات تجريبية
npm run db:seed

# 5. تشغيل المشروع
npm run dev
```

### هيكل الكود

#### المكونات (Components)

```
components/
├── shared/              # مكونات مشتركة
│   ├── Button.tsx
│   ├── Input.tsx
│   ├── Card.tsx
│   └── Modal.tsx
├── features/           # مكونات خاصة بالميزات
│   ├── orders/
│   ├── chat/
│   └── revisions/
└── pwa/                # مكونات PWA
    ├── PWAInstallPrompt.tsx
    └── OfflineIndicator.tsx
```

#### API Routes

```
app/api/
├── auth/               # المصادقة
│   ├── register/
│   └── login/
├── orders/             # الطلبات
│   ├── create/
│   └── [id]/
├── messages/           # الرسائل
│   └── send/
├── plans/               # المخططات
│   └── upload/
├── revisions/          # التعديلات
│   └── create/
└── notifications/       # الإشعارات
    └── subscribe/
```

### الأمان والاستقرار

#### الميزات المطبقة

1. **Rate Limiting**
   - 100 طلب/15 دقيقة للـ API العامة
   - 5 طلبات/15 دقيقة للمصادقة
   - Headers للـ Rate Limit (X-RateLimit-*)

2. **Security Headers**
   - HSTS (Strict-Transport-Security)
   - CSP (Content-Security-Policy)
   - X-Frame-Options, X-XSS-Protection
   - Referrer-Policy, Permissions-Policy

3. **Error Handling**
   - معالج أخطاء موحد (`lib/errors.ts`)
   - Error IDs للتتبع
   - إخفاء تفاصيل الأخطاء في الإنتاج

4. **Input Sanitization**
   - تنظيف المدخلات من HTML/scripts
   - استخدام `isomorphic-dompurify`

5. **Logging**
   - نظام Logging موحد (`lib/logger.ts`)
   - جاهز للتكامل مع Sentry/LogRocket

6. **Environment Validation**
   - التحقق من متغيرات البيئة (Zod)
   - رسائل خطأ واضحة

7. **Database Indexes**
   - فهارس محسّنة على جميع الجداول
   - تحسين أداء الاستعلامات

8. **Error Boundaries**
   - معالجة أخطاء React components
   - منع تعطل التطبيق بالكامل

### أفضل الممارسات

1. **استخدم TypeScript** - للسلامة والوضوح
2. **فصل الاهتمامات** - كل ملف له وظيفة واحدة
3. **استخدم Hooks** - لإعادة استخدام المنطق
4. **تحقق من المدخلات** - استخدم Zod للتحقق
5. **معالجة الأخطاء** - استخدم `handleApiError` من `lib/errors.ts`
6. **التوثيق** - اكتب تعليقات واضحة
7. **الأمان** - استخدم `sanitizeText` للمدخلات النصية
8. **Logging** - استخدم `logger` بدلاً من `console.log`

---

## قاعدة البيانات

### نوع قاعدة البيانات

**للـ MVP:** SQLite (ملف محلي `dev.db`)
**للإنتاج:** PostgreSQL (موصى به)

### إعداد قاعدة البيانات

```bash
# SQLite (للـ MVP)
DATABASE_URL="file:./dev.db"

# PostgreSQL (للإنتاج)
DATABASE_URL="postgresql://user:password@localhost:5432/fekra?schema=public"
```

### الجداول الرئيسية

#### User
- معلومات المستخدم الأساسية
- الدور (CLIENT, ENGINEER, ADMIN) - String (لتوافق SQLite)
- اشتراك Push Notifications (JSON)
- فهارس على: email, phone, role

#### Package
- معلومات الباقات
- السعر، عدد التعديلات، وقت التنفيذ
- حالة التفعيل

#### Order
- معلومات الطلب
- الحالة (PENDING, IN_PROGRESS, REVIEW, COMPLETED, ARCHIVED) - String
- التعديلات المتبقية
- الموعد النهائي
- المهندس المخصص (engineerId)
- بيانات الاستبيان (formData - JSON)

#### Plan
- المخططات المرفوعة
- رابط الملف
- نوع الملف (pdf, image)
- حالة النشاط (isActive)
- فهرس على: orderId

#### Message
- الرسائل في المحادثة
- المرسل (senderId)
- المحتوى
- حالة القراءة (isRead)
- فهارس على: orderId, senderId

#### RevisionRequest
- طلبات التعديل ✅ **مكتمل**
- الدبابيس (pins - JSON)
- الحالة (pending, in_progress, completed, rejected)
- فهارس على: orderId, status, createdAt

#### Notification
- الإشعارات ✅ **مكتمل (In-App)**
- النوع (message_received, plan_sent, revision_requested, etc.)
- البيانات الإضافية (data - JSON)
- فهارس على: userId, isRead, createdAt
- ⚠️ **ملاحظة:** Push Notifications (Web Push API) لم يتم تنفيذها بعد

### العلاقات

- User → Order (One-to-Many) - علاقتان: clientOrders, engineerOrders
- Package → Order (One-to-Many)
- Order → Plan (One-to-Many)
- Order → RevisionRequest (One-to-Many)
- Order → Message (One-to-Many)
- User → Message (One-to-Many) - MessageSender
- User → Notification (One-to-Many)

### ملاحظات SQLite

- لا يدعم SQLite Enums، لذلك تم استخدام String مع @default
- تم استخدام JSON fields لتخزين البيانات المعقدة (formData, pins, pushSubscription)
- جميع العلاقات محددة بوضوح باستخدام @relation

---

## API Reference

راجع [API.md](./API.md) للتوثيق الكامل لجميع API endpoints.

---

## التكاملات

### الدفع

- **Stripe** (موصى به)
- **PayPal**
- **مدى** (للسوق السعودي)

### التخزين

- **AWS S3** (موصى به)
- **Cloudinary** (للملفات)
- **Local Storage** (للتطوير فقط)

### الإشعارات

- **Web Push API**
- **Email** (اختياري - SendGrid, Resend)

---

## الأسئلة الشائعة

### كيف أضيف باقة جديدة؟

1. اذهب لوحة تحكم الإدارة
2. اضغط "إدارة الباقات"
3. اضغط "إضافة باقة جديدة"
4. املأ البيانات واحفظ

### كيف أغير كلمة المرور؟

1. اذهب للملف الشخصي
2. اضغط "تغيير كلمة المرور"
3. أدخل كلمة المرور القديمة والجديدة
4. احفظ

### كيف أطلب تعديلات إضافية؟

1. إذا نفدت التعديلات، ستظهر رسالة
2. اضغط "شراء تعديلات إضافية"
3. اختر عدد التعديلات
4. ادفع
5. سيتم إضافة التعديلات وتمديد الموعد

---

## الدعم

للأسئلة والدعم:
- افتح Issue في GitHub
- راجع [TIPS.md](./TIPS.md) للنصائح
- راجع [ARCHITECTURE.md](./ARCHITECTURE.md) للمعمارية
- راجع [STATUS_REPORT.md](./STATUS_REPORT.md) لتقرير الحالة الكامل
- راجع [IMPLEMENTED.md](./IMPLEMENTED.md) لتفاصيل ما تم تنفيذه
