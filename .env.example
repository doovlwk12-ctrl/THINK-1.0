# منصة فكرة — يعمل محلياً أو على Vercel + Supabase

# Database — PostgreSQL
# محلي: postgresql://user:password@localhost:5432/think_db
# Supabase على Vercel: استخدم وضع Transaction (منفذ 6543) مع ?pgbouncer=true — راجع docs/VERCEL-SUPABASE-CHECKLIST.md
# مثال Vercel: postgres://postgres:PASSWORD@db.xxxxx.supabase.co:6543/postgres?pgbouncer=true
DATABASE_URL="postgresql://user:password@localhost:5432/think_db"

# NextAuth — مصادقة محلية
NEXTAUTH_SECRET="your-secret-key-here-minimum-32-characters"
NEXTAUTH_URL="http://localhost:3000"

# للتشغيل المحلي: لا تضف أي متغيرات Supabase أدناه (أو اتركها معطلة).
# إذا أضفت NEXT_PUBLIC_USE_SUPABASE_AUTH="true" فسيحاول التطبيق الاتصال بـ Supabase وسيفشل بدون مشروع نشط.
# NEXT_PUBLIC_USE_SUPABASE_AUTH=

NODE_ENV="development"

# Optional: لتأمين GET /api/system/health — استخدم هيدر x-health-secret أو ?secret=VALUE
# HEALTH_CHECK_SECRET="your-random-secret"
